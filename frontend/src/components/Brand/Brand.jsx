function Brand({ className = 'mb-10' }) {
  return (
    <span className={`${className} block font-mono text-sm font-medium text-text-primary`}>
      C4//diagrams
    </span>
  );
}

export default Brand;